import type { NextConfig } from "next";

// Catatan Penting: 
// Proyek ini memiliki file `next.config.js` dan `next.config.ts`.
// Biasanya Next.js hanya akan membaca SATU file konfigurasi saja (memprioritaskan ekstensi .js atau .ts berdasarkan versi).
// Sangat disarankan untuk menghapus salah satu file agar tidak terjadi konflik konfigurasi di masa depan.

// Ini adalah kerangka konfigurasi dalam format TypeScript
const nextConfig: NextConfig = {
  /* Opsi konfigurasi tambahan bisa diletakkan di sini (mirip seperti yang ada di next.config.js) */
};

export default nextConfig;
